@model IEnumerable<mcknaldi.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2>Categorieën</h2>

<table class="table">
    <tr>
        <th>
            Hoofd categorie
        </th>
        <th>
            Sub categorie
        </th>
        <th>
            Subsub categorie
        </th>
        <th>
            Afbeelding
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        if (item.ParentId == null)
        {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            -
        </td>
        <td>
            -
        </td>
        <td>
            <img src="~/Uploads/Content/@item.Id/@item.Image" style="width:100px;height:auto;" />
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id })
        </td>
    </tr>
            foreach (var item1 in Model)
            {
                if (item1.ParentId == item.Id)
                {
    <tr>
        <td></td>
        <td>
            @Html.DisplayFor(modelItem => item1.Name)
        </td>
        <td>
            -
        </td>
        <td>
            <img src="~/Uploads/Content/@item1.Id/@item1.Image" style="width:100px;height:auto;" />
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item1.Id })
        </td>
    </tr>
                    foreach (var item2 in Model)
                    {
                        if (item2.ParentId == item1.Id)
                        {
    <tr>
        <td></td>
        <td></td>
        <td>
            @Html.DisplayFor(modelItem => item2.Name)
        </td>
        <td>
            <img src="~/Uploads/Content/@item2.Id/@item2.Image" style="width:100px;height:auto;" />
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item2.Id })
        </td>
    </tr>
                        }
                    }

                }

            }
        }        
                       
    }

</table>
