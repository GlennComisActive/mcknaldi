@model IEnumerable<mcknaldi.Models.Category>

@{
    ViewBag.Title = "Categories";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>

        @foreach (var item in Model)
        {
            if (item.ParentId == null)
            {

             <button>@Html.DisplayFor(modelItem => item.Name)</button>

            }
            
        }
</body>
</html>
